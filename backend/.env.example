PORT=3000
HOST=0.0.0.0
DB_CLIENT=mysql
DB_HOST=localhost
DB_PORT=3306
DB_NAME=certigen
DB_USER=root
DB_PASSWORD=
DB_SSL=false
# For free hosted Postgres providers such as Neon:
# DB_CLIENT=postgres
# DATABASE_URL=postgresql://user:password@host/database?sslmode=require
# Or use one hosted MySQL connection URL instead of the DB_* fields:
# DATABASE_URL=mysql://user:password@host:3306/database
EMAIL_USER=
EMAIL_PASS=
PUBLIC_BASE_URL=
ADMIN_NAME=Admin User
ADMIN_EMAIL=admin@certigen.local
ADMIN_PASSWORD=change-this-before-deploying
ADMIN_PHONE=
CREATE_DEMO_EMPLOYEE=false
DEMO_EMPLOYEE_EMAIL=employee@certigen.local
DEMO_EMPLOYEE_PASSWORD=change-this-too
TRUST_PROXY=true
# Optional: move generated runtime files to a mounted disk or temp folder.
DATA_DIR=
